void setup(){
  background(0);
  size(500,500);
  frameRate(15);
}

void draw(){
 
poo();
}
void poo(){
 int x=20;
  while(x<480){
  noStroke();
  fill(random(250),random(240),random(230));
    ellipse(mouseX,250,25,-mouseY);
  x=x+30;
 }
